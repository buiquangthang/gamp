<ul class="nav nav-tabs nav-justified nav-custom" id="bus-line-info">
  <li class="active">
    <a data-toggle="tab" href="#direction-detail">
      <i class="fa fa-bus" aria-hidden="true"></i>
      Chi tiet cach di
    </a>
  </li>
  <li>
    <a data-toggle="tab" href="#bus-station-over">
      <i class="fa fa-info-circle"></i>
      Cac tram di qua  
    </a>
  </li>
</ul>


<div class="tab-content">
  <div id="direction-detail" class="tab-pane fade in active">
    <div href="#" class="list-group-item">
      <div class="row">
        <% if @shortest_path.present? %>
          <h3>Xin lỗi, hệ thống không tìm thấy đường đi phù hợp với yêu cầu</h3>
        <% else %>
          <%= render "direction_step" %>
        <% end %>
      </div>
    </div>
  </div>
  <div id="bus-station-over" class="tab-pane fade">
    <% if @bus_stations %>
      <% @bus_stations.each do |bus_station| %>
        <a href="#" class="list-group-item marker-link" data-markerid="<%= bus_station.id %>">
          <h4><%= bus_station.address %></h4>
        </a>
      <% end %>
    <% end %>
  </div>
</div>

